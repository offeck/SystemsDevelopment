{
  "name": "BGU SPL Student Workspace",
  "image": "ghcr.io/bguspl/student-env:latest",
  "remoteUser": "spl",
  "runArgs": ["--cap-add=SYS_PTRACE", "--security-opt", "seccomp=unconfined"],
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.cpptools-extension-pack",
        "vscjava.vscode-java-pack",
        "ms-python.python",
        "mtxr.sqltools",
        "mtxr.sqltools-driver-sqlite",
        "ms-vsliveshare.vsliveshare",
        "vscode-icons-team.vscode-icons",
        "2gua.rainbow-brackets",
        "jeff-hykin.better-cpp-syntax",
        "ms-vscode.makefile-tools",
        "natqe.reload",
        "cschlosser.doxdocgen"
      ],
      "settings": {
        "terminal.integrated.defaultProfile.linux": "bash"
      }
    }
  },
  "mounts": ["source=${localWorkspaceFolder},target=/workspace,type=bind,consistency=cached"],
  "workspaceFolder": "/workspace",
  "postCreateCommand": "bash -lc 'if [ -f requirements.txt ]; then pip install -r requirements.txt; fi; echo \"\"; echo \"═══════════════════════════════════════════════════════════\"; echo \"  Welcome to BGU SPL Student Env (v${STUDENT_ENV_VERSION})\"; echo \"═══════════════════════════════════════════════════════════\"; echo \"\"; echo \"Available tools:\"; echo \"  • C/C++: gcc-13, g++-13, clang, cmake\"; echo \"  • Java: OpenJDK 21 (Temurin)\"; echo \"  • Maven: $(mvn -version 2>/dev/null | head -n1 | cut -d\" \" -f3 || echo \"Not found\")\"; echo \"  • Python: 3.12\"; echo \"  • SQLite: sqlite3\"; echo \"\"; java -version 2>&1 | head -n1; mvn -version 2>&1 | head -n1 || echo \"⚠️  Maven not found in PATH\"; echo \"\"; echo \"✅ Container is ready! Happy coding! 🚀\"; echo \"\"'"
}
